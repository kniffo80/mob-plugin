# this is the configuration file for the plugin. this needs to be put in the plugin folder!
entities:
 # mobs will move and aggress when set to true
 mob-ai: true
 # set this to 0 to not start the spawn thread
 auto-spawn-tick: 300

# configure max spawns of animals / mobs
spawn-config:
 max-wolf: 1